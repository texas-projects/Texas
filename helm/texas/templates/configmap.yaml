apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "texas.configMapName" . }}
  labels:
    {{- include "texas.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
